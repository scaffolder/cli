name: scaffolder
version: '0.1.4' # just for humans, typically '1.2+git' or '1.3.2'
summary: The future of apps development.
description: |
  High level project, application and microservices modeling tool based on the rules of Scaffolder DSL which utilizes Scaffolder aPaaS to generate all source code, configuration and testing files.
grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots
parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil